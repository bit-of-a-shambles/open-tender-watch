# SQLite. Versions 3.8.0 and up are supported.
#   gem install sqlite3
#
#   Ensure the SQLite 3 gem is defined in your Gemfile
#   gem "sqlite3"
#
default: &default
  adapter: sqlite3
  pool: <%= ENV.fetch("RAILS_MAX_THREADS") { 5 } %>
  timeout: 30000

development:
  <<: *default
  database: storage/development.sqlite3
  pragmas:
    journal_mode: wal
    synchronous: normal
    busy_timeout: 30000

# Warning: The database defined as "test" will be erased and
# re-generated from your development database when you run "rake".
# Do not set this db to the same as development or production.
test:
  <<: *default
  database: storage/test.sqlite3


# Store production database in the storage/ directory, which by default
# is mounted as a persistent Docker volume in config/deploy.yml.
production:
  primary:
    <<: *default
    database: storage/production.sqlite3
    pragmas:
      journal_mode: wal        # multiple concurrent readers; single writer serialised
      synchronous: normal      # safe with WAL — only lose the last transaction on crash
      busy_timeout: 30000      # wait up to 30s for a write lock before raising BUSY
      cache_size: -20000       # 20MB page cache per connection
      mmap_size: 134217728     # 128MB memory-mapped I/O for faster reads
  cache:
    <<: *default
    database: storage/production_cache.sqlite3
    migrations_paths: db/cache_migrate
    pragmas:
      journal_mode: wal
      synchronous: normal
      busy_timeout: 30000
  queue:
    <<: *default
    database: storage/production_queue.sqlite3
    migrations_paths: db/queue_migrate
    pragmas:
      journal_mode: wal
      synchronous: normal
      busy_timeout: 30000
  cable:
    <<: *default
    database: storage/production_cable.sqlite3
    migrations_paths: db/cable_migrate
    pragmas:
      journal_mode: wal
      synchronous: normal
      busy_timeout: 30000
